server {
    listen 80;
    server_name localhost;

    location / {
        default_type text/plain;
        return 200 "Nginx Proxy Manager placeholder - Security Gateway is not yet routing here.\n";
    }
}
